export function Footer() {
  return (
    <footer className="mt-8 border-t border-[var(--border)] py-6 text-center text-xs text-[var(--muted)]">
      Built for fun. Client-side only. Export your meme and share it anywhere.
    </footer>
  );
}
