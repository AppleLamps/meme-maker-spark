import type { MemeStyle } from "@/types/meme";

export const DEFAULT_TEMPLATE_ID = "drake-hotline-bling";

export const DEFAULT_STYLE: MemeStyle = {
  fontFamily: "Impact",
  fontSize: 54,
  textColor: "#ffffff",
  strokeColor: "#000000",
  strokeWidth: 4,
  shadowEnabled: true,
};
